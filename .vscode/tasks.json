{
  "tasks": [
    {
      "type": "shell",
      "label": "gcc build active file",
      "command": "make debug",
      "args": [],
      "options": {
        "cwd": "${fileDirname}"
      }
    },
    {
      "type": "shell",
      "label": "gcc build & run active file",
      "command": "make",
      "args": [],
      "options": {
        "cwd": "${fileDirname}"
      }
    }
  ],
  "version": "2.0.0"
}
